
import { MathConcept } from '../src/math-concept.js'

/**
 * The Expression class, still just a stub for now.
 */
export class Expression extends MathConcept {
    
    static className = MathConcept.addSubclass( 'Expression', Expression )
    
}
