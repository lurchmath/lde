
import { Structure } from '../src/structure.js'

/**
 * The Expression class, still just a stub for now.
 */
export class Expression extends Structure {
    
    static className = Structure.addSubclass( 'Expression', Expression )
    
}
