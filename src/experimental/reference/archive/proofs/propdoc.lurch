//////////////////////////////////////////////
// Default Lode Propositional Document
//
{ (--- "Theorem: If ¬(P or Q) then ¬P and ¬Q")
  { :(¬ (or P Q)) (and (¬ P) (¬ Q)) } 
  (--- "Proof:")
  
  { :(¬ (or P Q) )
     { :P (or P Q) →← } (¬ P)
     { :Q (or P Q) →← } (¬ Q) 
  }
}