////////////////////////////////////////////////////////////////////
// Preemie Tests Lib
// 
// This is a simple test for the preemie checker

{
  // Declare
  :[ ∀ ∃ = ∃! 𝜎 = ]
  
  // ∀+
  :{ :{ :[x] (@ P x) }  (∀ y,(@ P y)) } // ∀+

  :{ (= x x) }

}