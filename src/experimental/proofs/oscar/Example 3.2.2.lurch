include libs/Reals
{
	Definition: :{ x^2=x⋅x }
	{
    Let n be such that n is even
    n = 2⋅k for some k
    
		n^2 = n⋅n
		    = (2⋅k)⋅n
				= (2⋅k)⋅(2⋅k)
				= 2⋅(k⋅(2⋅k))
    
		n^2 is even
  }
}
