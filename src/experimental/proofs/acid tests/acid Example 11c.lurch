////////////////////////////////////////////////////////////////////
// Acid Test Proofs
// 
// These are the example and counterexample proofs from the END document

(<<< "Example 11c from END")
{
   (<<< "Trying even harder to cheat the subset plus rule.")
   // Thm/Pf attempt 
   { { :[ x ] :(∈ x T) } ✗
     (<<< "Expected to fail here.")
     (⊆ S T) ✗
   }
}