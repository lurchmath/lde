////////////////////////////////////////////////////////////////////
// Acid Test Proofs
// 
// These are the example and counterexample proofs from the END document

(<<< "Example 11b from END")
{
   (<<< "Trying again to cheat the subset plus rule.")
   (<<< "Note this DOES validate if we define ⊆+ using a Let with no body!")
   // Thm/Pf attempt 
   { :[ x , (⊆ S T) ] :(∈ x T) 
     (⊆ S T)
   }
}