////////////////////////////////////////////////////////////////////
// Acid Test Proofs
// 
// These are the example and counterexample proofs from the END document

(--- "Example 9 from END")
{
   (--- "Thm: 1<1 (generalizing inside a Let environment)")
   { 
     {:[ z ]
       {:(< 2 z)
         (< 1 z) 
         (∀ x , (< 1 x))  // the copy of this in the headless should be invalid
                          // but it's not because the instantiations from below
                          // actually justify it!
       }
      (⇒ (< 2 z) (∀ x , (< 1 x)))
     } ✔︎
     (∀ y , (⇒ (< 2 y) (∀ x , (< 1 x))) ) ✔︎
     (⇒ (< 2 3) (∀ x , (< 1 x))) ✔︎
     (∀ x , (< 1 x)) ✔
     (< 1 1) ✔︎
   } 
}