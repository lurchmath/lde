
import { InputStructure } from '../src/input-structure.js'

describe( 'InputStructure', () => {

    it( 'Ensure all expected global identifiers are declared', () => {
        expect( InputStructure ).to.be.ok
    } )

} )
